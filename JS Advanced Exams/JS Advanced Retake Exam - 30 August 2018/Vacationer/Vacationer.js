class Vacationer {
    constructor(name, card) {
        this.fullname = `${name[0]} ${name[1]} ${name[2]}`;
        this.idNumber = '';
        this.creditCard = card
        this.wishList = [];
        };
    }
}
